tomosphero - 0.0.3
------------------
- view geometries contain optional masks
- reduce memory consumption by flattening indices/lens arrays
- add `HuberLoss` for more robust fidelity term
- add `SphericalGrid.static` for deriving static grid from dynamic

tomosphero - 0.0.2
------------------
- support plotting of grids without 100% coverage
- add tutorial, improve docs